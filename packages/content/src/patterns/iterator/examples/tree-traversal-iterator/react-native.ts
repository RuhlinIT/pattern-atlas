import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Tree traversal iterator",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\n\nclass TreeNode {\n  constructor(\n    public value: string,\n    public left: TreeNode | null = null,\n    public right: TreeNode | null = null\n  ) {}\n}\n\n\nclass InOrderIterator {\n  private stack: TreeNode[] = [];\n\n\n  constructor(root: TreeNode | null) {\n    this.pushLeft(root);\n  }\n\n\n  private pushLeft(node: TreeNode | null): void {\n    while (node) {\n      this.stack.push(node);\n      node = node.left;\n    }\n  }\n\n\n  next(): string | null {\n    if (!this.hasNext()) {\n      return null;\n    }\n\n\n    const node = this.stack.pop()!;\n    this.pushLeft(node.right);\n    return node.value;\n  }\n\n\n  hasNext(): boolean {\n    return this.stack.length > 0;\n  }\n}\n\n\nfunction TreeView({ iterator }: { iterator: InOrderIterator }) {\n  const values: string[] = [];\n  while (iterator.hasNext()) {\n    const value = iterator.next();\n    if (value) values.push(value);\n  }\n\n\n  return (\n    <View>\n      <Text>{values.join(\", \")}</Text>\n    </View>\n  );\n}\n\n\nexport function App() {\n  const iterator = useMemo(() => {\n    const root = new TreeNode(\n      \"A\",\n      new TreeNode(\"B\", new TreeNode(\"D\"), new TreeNode(\"E\")),\n      new TreeNode(\"C\", null, new TreeNode(\"F\"))\n    );\n    return new InOrderIterator(root);\n  }, []);\n\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Tree Traversal Iterator</Text>\n        <TreeView iterator={iterator} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native version walks the tree in order and presents the visited values in a simple mobile layout.",
};
