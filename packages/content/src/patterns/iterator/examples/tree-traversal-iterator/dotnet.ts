import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const dotnet: PatternLanguageExample = {
  language: "dotnet",
  title: "Tree traversal iterator",
  code: "using System;\nusing System.Collections.Generic;\nusing Microsoft.Extensions.DependencyInjection;\n\n\npublic class TreeNode\n{\n    public string Value { get; }\n    public TreeNode Left { get; }\n    public TreeNode Right { get; }\n\n\n    public TreeNode(string value, TreeNode left = null, TreeNode right = null)\n    {\n        Value = value;\n        Left = left;\n        Right = right;\n    }\n}\n\n\npublic class InOrderIterator\n{\n    private readonly Stack<TreeNode> _stack = new Stack<TreeNode>();\n\n\n    public InOrderIterator(TreeNode root)\n    {\n        PushLeft(root);\n    }\n\n\n    private void PushLeft(TreeNode node)\n    {\n        while (node != null)\n        {\n            _stack.Push(node);\n            node = node.Left;\n        }\n    }\n\n\n    public string Next()\n    {\n        if (!HasNext())\n        {\n            return null;\n        }\n\n\n        var node = _stack.Pop();\n        PushLeft(node.Right);\n        return node.Value;\n    }\n\n\n    public bool HasNext()\n    {\n        return _stack.Count > 0;\n    }\n}\n\n\nvar services = new ServiceCollection();\nservices.AddSingleton(new TreeNode(\n    \"A\",\n    new TreeNode(\"B\", new TreeNode(\"D\"), new TreeNode(\"E\")),\n    new TreeNode(\"C\", null, new TreeNode(\"F\"))\n));\n\n\nvar provider = services.BuildServiceProvider();\nvar iterator = new InOrderIterator(provider.GetRequiredService<TreeNode>());\n\n\nwhile (iterator.HasNext())\n{\n    Console.WriteLine(iterator.Next());\n}",
  explanation: "The .NET example uses dependency injection to provide the tree, then iterates through it without exposing traversal internals.",
};
