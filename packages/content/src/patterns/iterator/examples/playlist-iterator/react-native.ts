import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Playlist iterator",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\n\ninterface Iterator<T> {\n  next(): T | null;\n  hasNext(): boolean;\n}\n\n\nclass PlaylistIterator implements Iterator<string> {\n  private index = 0;\n\n\n  constructor(private songs: string[]) {}\n\n\n  next(): string | null {\n    if (!this.hasNext()) {\n      return null;\n    }\n\n\n    return this.songs[this.index++];\n  }\n\n\n  hasNext(): boolean {\n    return this.index < this.songs.length;\n  }\n}\n\n\nclass Playlist {\n  constructor(private songs: string[]) {}\n\n\n  createIterator(): Iterator<string> {\n    return new PlaylistIterator(this.songs);\n  }\n}\n\n\nfunction PlaylistView({ playlist }: { playlist: Playlist }) {\n  const iterator = useMemo(() => playlist.createIterator(), [playlist]);\n\n\n  const songs: string[] = [];\n  while (iterator.hasNext()) {\n    const song = iterator.next();\n    if (song) songs.push(song);\n  }\n\n\n  return (\n    <View>\n      <Text>{songs.join(\", \")}</Text>\n    </View>\n  );\n}\n\n\nexport function App() {\n  const playlist = useMemo(\n    () => new Playlist([\"Intro\", \"First Song\", \"Second Song\"]),\n    []\n  );\n\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Playlist Iterator</Text>\n        <PlaylistView playlist={playlist} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native example iterates through playlist items in a mobile-friendly UI while preserving encapsulation.",
};
