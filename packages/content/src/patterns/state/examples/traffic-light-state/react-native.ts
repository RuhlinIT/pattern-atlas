import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Traffic light state",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\n\ninterface TrafficLightState {\n  next(light: TrafficLight): void;\n  name(): string;\n}\n\n\nclass TrafficLight {\n  private state: TrafficLightState;\n\n\n  constructor() {\n    this.state = new RedState();\n  }\n\n\n  setState(state: TrafficLightState): void {\n    this.state = state;\n  }\n\n\n  change(): void {\n    this.state.next(this);\n  }\n\n\n  getStateName(): string {\n    return this.state.name();\n  }\n}\n\n\nclass RedState implements TrafficLightState {\n  next(light: TrafficLight): void {\n    light.setState(new GreenState());\n  }\n\n\n  name(): string {\n    return \"Red\";\n  }\n}\n\n\nclass GreenState implements TrafficLightState {\n  next(light: TrafficLight): void {\n    light.setState(new YellowState());\n  }\n\n\n  name(): string {\n    return \"Green\";\n  }\n}\n\n\nclass YellowState implements TrafficLightState {\n  next(light: TrafficLight): void {\n    light.setState(new RedState());\n  }\n\n\n  name(): string {\n    return \"Yellow\";\n  }\n}\n\n\nfunction LightPreview({ light }: { light: TrafficLight }) {\n  return (\n    <View>\n      <Text>{light.getStateName()}</Text>\n    </View>\n  );\n}\n\n\nexport function App() {\n  const light = useMemo(() => new TrafficLight(), []);\n\n\n  useMemo(() => {\n    light.change();\n  }, [light]);\n\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Traffic Light State</Text>\n        <LightPreview light={light} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native example displays the current light color while the state classes manage the sequence behind the scenes.",
};
