import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const react: PatternLanguageExample = {
  language: "react",
  title: "Traffic light state",
  code: "import React, { useMemo } from \"react\";\n\n\ninterface TrafficLightState {\n  next(light: TrafficLight): void;\n  name(): string;\n}\n\n\nclass TrafficLight {\n  private state: TrafficLightState;\n\n\n  constructor() {\n    this.state = new RedState();\n  }\n\n\n  setState(state: TrafficLightState): void {\n    this.state = state;\n  }\n\n\n  change(): void {\n    this.state.next(this);\n  }\n\n\n  getStateName(): string {\n    return this.state.name();\n  }\n}\n\n\nclass RedState implements TrafficLightState {\n  next(light: TrafficLight): void {\n    light.setState(new GreenState());\n  }\n\n\n  name(): string {\n    return \"Red\";\n  }\n}\n\n\nclass GreenState implements TrafficLightState {\n  next(light: TrafficLight): void {\n    light.setState(new YellowState());\n  }\n\n\n  name(): string {\n    return \"Green\";\n  }\n}\n\n\nclass YellowState implements TrafficLightState {\n  next(light: TrafficLight): void {\n    light.setState(new RedState());\n  }\n\n\n  name(): string {\n    return \"Yellow\";\n  }\n}\n\n\nfunction LightPreview({ light }: { light: TrafficLight }) {\n  return <p>{light.getStateName()}</p>;\n}\n\n\nexport function App() {\n  const light = useMemo(() => new TrafficLight(), []);\n\n\n  useMemo(() => {\n    light.change();\n  }, [light]);\n\n\n  return (\n    <main>\n      <h1>Traffic Light State</h1>\n      <LightPreview light={light} />\n    </main>\n  );\n}",
  explanation: "The React example reads the current traffic light state while the state objects themselves control transitions.",
};
