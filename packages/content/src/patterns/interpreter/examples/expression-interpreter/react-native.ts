import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Expression interpreter",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\n\ninterface Expression {\n  interpret(): number;\n}\n\n\nclass NumberExpression implements Expression {\n  constructor(private value: number) {}\n\n\n  interpret(): number {\n    return this.value;\n  }\n}\n\n\nclass AddExpression implements Expression {\n  constructor(private left: Expression, private right: Expression) {}\n\n\n  interpret(): number {\n    return this.left.interpret() + this.right.interpret();\n  }\n}\n\n\nclass SubtractExpression implements Expression {\n  constructor(private left: Expression, private right: Expression) {}\n\n\n  interpret(): number {\n    return this.left.interpret() - this.right.interpret();\n  }\n}\n\n\nclass MultiplyExpression implements Expression {\n  constructor(private left: Expression, private right: Expression) {}\n\n\n  interpret(): number {\n    return this.left.interpret() * this.right.interpret();\n  }\n}\n\n\nfunction ResultPreview({ expression }: { expression: Expression }) {\n  return (\n    <View>\n      <Text>{expression.interpret()}</Text>\n    </View>\n  );\n}\n\n\nexport function App() {\n  const expression = useMemo(\n    () =>\n      new SubtractExpression(\n        new AddExpression(new NumberExpression(5), new NumberExpression(3)),\n        new MultiplyExpression(new NumberExpression(2), new NumberExpression(4))\n      ),\n    []\n  );\n\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Expression Interpreter</Text>\n        <ResultPreview expression={expression} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native example evaluates the same expression tree and shows the interpreted result in a mobile UI.",
};
