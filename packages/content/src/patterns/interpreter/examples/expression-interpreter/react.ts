import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const react: PatternLanguageExample = {
  language: "react",
  title: "Expression interpreter",
  code: "import React, { useMemo } from \"react\";\n\n\ninterface Expression {\n  interpret(): number;\n}\n\n\nclass NumberExpression implements Expression {\n  constructor(private value: number) {}\n\n\n  interpret(): number {\n    return this.value;\n  }\n}\n\n\nclass AddExpression implements Expression {\n  constructor(private left: Expression, private right: Expression) {}\n\n\n  interpret(): number {\n    return this.left.interpret() + this.right.interpret();\n  }\n}\n\n\nclass SubtractExpression implements Expression {\n  constructor(private left: Expression, private right: Expression) {}\n\n\n  interpret(): number {\n    return this.left.interpret() - this.right.interpret();\n  }\n}\n\n\nclass MultiplyExpression implements Expression {\n  constructor(private left: Expression, private right: Expression) {}\n\n\n  interpret(): number {\n    return this.left.interpret() * this.right.interpret();\n  }\n}\n\n\nfunction ResultPreview({ expression }: { expression: Expression }) {\n  return <p>{expression.interpret()}</p>;\n}\n\n\nexport function App() {\n  const expression = useMemo(\n    () =>\n      new SubtractExpression(\n        new AddExpression(new NumberExpression(5), new NumberExpression(3)),\n        new MultiplyExpression(new NumberExpression(2), new NumberExpression(4))\n      ),\n    []\n  );\n\n\n  return (\n    <main>\n      <h1>Expression Interpreter</h1>\n      <ResultPreview expression={expression} />\n    </main>\n  );\n}",
  explanation: "The React example keeps the interpreter logic inside expression objects while the UI only renders the final computed result.",
};
