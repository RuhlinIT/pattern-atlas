import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const dotnet: PatternLanguageExample = {
  language: "dotnet",
  title: "Expression interpreter",
  code: "using System;\nusing Microsoft.Extensions.DependencyInjection;\n\n\npublic interface IExpression\n{\n    int Interpret();\n}\n\n\npublic class NumberExpression : IExpression\n{\n    private readonly int _value;\n\n\n    public NumberExpression(int value)\n    {\n        _value = value;\n    }\n\n\n    public int Interpret()\n    {\n        return _value;\n    }\n}\n\n\npublic class AddExpression : IExpression\n{\n    private readonly IExpression _left;\n    private readonly IExpression _right;\n\n\n    public AddExpression(IExpression left, IExpression right)\n    {\n        _left = left;\n        _right = right;\n    }\n\n\n    public int Interpret()\n    {\n        return _left.Interpret() + _right.Interpret();\n    }\n}\n\n\npublic class SubtractExpression : IExpression\n{\n    private readonly IExpression _left;\n    private readonly IExpression _right;\n\n\n    public SubtractExpression(IExpression left, IExpression right)\n    {\n        _left = left;\n        _right = right;\n    }\n\n\n    public int Interpret()\n    {\n        return _left.Interpret() - _right.Interpret();\n    }\n}\n\n\npublic class MultiplyExpression : IExpression\n{\n    private readonly IExpression _left;\n    private readonly IExpression _right;\n\n\n    public MultiplyExpression(IExpression left, IExpression right)\n    {\n        _left = left;\n        _right = right;\n    }\n\n\n    public int Interpret()\n    {\n        return _left.Interpret() * _right.Interpret();\n    }\n}\n\n\nvar services = new ServiceCollection();\nservices.AddSingleton<IExpression>(\n    new SubtractExpression(\n        new AddExpression(new NumberExpression(5), new NumberExpression(3)),\n        new MultiplyExpression(new NumberExpression(2), new NumberExpression(4))\n    )\n);\n\n\nvar provider = services.BuildServiceProvider();\nvar expression = provider.GetRequiredService<IExpression>();\n\nConsole.WriteLine(expression.Interpret());",
  explanation: "The .NET example registers the expression tree in dependency injection so the interpreter can be resolved and evaluated like any other service.",
};
