import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const dotnet: PatternLanguageExample = {
  language: "dotnet",
  title: "Stock price alerts",
  code: "using System;\nusing System.Collections.Generic;\nusing Microsoft.Extensions.DependencyInjection;\n\npublic interface IObserver\n{\n    void Update(decimal price);\n}\n\npublic class Stock\n{\n    private readonly List<IObserver> _observers = new();\n    private decimal _price;\n\n    public Stock(decimal price)\n    {\n        _price = price;\n    }\n\n    public void Subscribe(IObserver observer)\n    {\n        _observers.Add(observer);\n    }\n\n    public void Unsubscribe(IObserver observer)\n    {\n        _observers.Remove(observer);\n    }\n\n    public void SetPrice(decimal price)\n    {\n        _price = price;\n        Notify();\n    }\n\n    private void Notify()\n    {\n        foreach (var observer in _observers)\n        {\n            observer.Update(_price);\n        }\n    }\n}\n\npublic class PriceDisplay : IObserver\n{\n    public void Update(decimal price)\n    {\n        Console.WriteLine($\"Display updated: {price}\");\n    }\n}\n\npublic class PriceAlert : IObserver\n{\n    public void Update(decimal price)\n    {\n        if (price > 100)\n        {\n            Console.WriteLine($\"Alert: stock price is {price}\");\n        }\n    }\n}\n\nvar services = new ServiceCollection();\nservices.AddSingleton(new Stock(95m));\n\nvar provider = services.BuildServiceProvider();\nvar stock = provider.GetRequiredService<Stock>();\nstock.Subscribe(new PriceDisplay());\nstock.Subscribe(new PriceAlert());\nstock.SetPrice(105m);",
  explanation: "The .NET version shows the same observer pattern with dependency injection available for the subject, while display and alert observers remain decoupled from the stock's internal state changes.",
};
