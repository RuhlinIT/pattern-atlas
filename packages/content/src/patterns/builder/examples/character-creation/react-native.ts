import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Character creation",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\nclass Character {\n  constructor(\n    public name: string,\n    public classType: string,\n    public strength: number,\n    public agility: number,\n    public intelligence: number,\n  ) {}\n}\n\nclass CharacterBuilder {\n  private name = \"Unnamed\";\n  private classType = \"Adventurer\";\n  private strength = 5;\n  private agility = 5;\n  private intelligence = 5;\n\n  withName(name: string): this {\n    this.name = name;\n    return this;\n  }\n\n  withClassType(classType: string): this {\n    this.classType = classType;\n    return this;\n  }\n\n  withStrength(strength: number): this {\n    this.strength = strength;\n    return this;\n  }\n\n  withAgility(agility: number): this {\n    this.agility = agility;\n    return this;\n  }\n\n  withIntelligence(intelligence: number): this {\n    this.intelligence = intelligence;\n    return this;\n  }\n\n  build(): Character {\n    return new Character(\n      this.name,\n      this.classType,\n      this.strength,\n      this.agility,\n      this.intelligence,\n    );\n  }\n}\n\nfunction CharacterCard({ character }: { character: Character }) {\n  return (\n    <View>\n      <Text>{character.name}</Text>\n      <Text>{character.classType}</Text>\n      <Text>STR: {character.strength}</Text>\n      <Text>AGI: {character.agility}</Text>\n      <Text>INT: {character.intelligence}</Text>\n    </View>\n  );\n}\n\nexport function App() {\n  const character = useMemo(\n    () =>\n      new CharacterBuilder()\n        .withName(\"Aria\")\n        .withClassType(\"Mage\")\n        .withStrength(3)\n        .withAgility(7)\n        .withIntelligence(10)\n        .build(),\n    [],\n  );\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Character Builder</Text>\n        <CharacterCard character={character} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native example uses the builder to prepare the character object first, then displays it in a mobile-friendly layout.",
};
