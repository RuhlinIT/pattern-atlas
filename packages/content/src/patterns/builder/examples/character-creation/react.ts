import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const react: PatternLanguageExample = {
  language: "react",
  title: "Character creation",
  code: "import React, { useMemo } from \"react\";\n\nclass Character {\n  constructor(\n    public name: string,\n    public classType: string,\n    public strength: number,\n    public agility: number,\n    public intelligence: number,\n  ) {}\n}\n\nclass CharacterBuilder {\n  private name = \"Unnamed\";\n  private classType = \"Adventurer\";\n  private strength = 5;\n  private agility = 5;\n  private intelligence = 5;\n\n  withName(name: string): this {\n    this.name = name;\n    return this;\n  }\n\n  withClassType(classType: string): this {\n    this.classType = classType;\n    return this;\n  }\n\n  withStrength(strength: number): this {\n    this.strength = strength;\n    return this;\n  }\n\n  withAgility(agility: number): this {\n    this.agility = agility;\n    return this;\n  }\n\n  withIntelligence(intelligence: number): this {\n    this.intelligence = intelligence;\n    return this;\n  }\n\n  build(): Character {\n    return new Character(\n      this.name,\n      this.classType,\n      this.strength,\n      this.agility,\n      this.intelligence,\n    );\n  }\n}\n\nfunction CharacterCard({ character }: { character: Character }) {\n  return (\n    <div>\n      <p>{character.name}</p>\n      <p>{character.classType}</p>\n      <p>STR: {character.strength}</p>\n      <p>AGI: {character.agility}</p>\n      <p>INT: {character.intelligence}</p>\n    </div>\n  );\n}\n\nexport function App() {\n  const character = useMemo(\n    () =>\n      new CharacterBuilder()\n        .withName(\"Aria\")\n        .withClassType(\"Mage\")\n        .withStrength(3)\n        .withAgility(7)\n        .withIntelligence(10)\n        .build(),\n    [],\n  );\n\n  return (\n    <main>\n      <h1>Character Builder</h1>\n      <CharacterCard character={character} />\n    </main>\n  );\n}",
  explanation: "The React example builds the character before rendering it, which keeps optional setup out of the component tree.",
};
