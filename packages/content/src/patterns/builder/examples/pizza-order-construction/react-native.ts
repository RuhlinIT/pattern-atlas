import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Pizza order construction",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\nclass Pizza {\n  constructor(\n    public size: string,\n    public crust: string,\n    public cheese: boolean,\n    public toppings: string[],\n  ) {}\n}\n\nclass PizzaBuilder {\n  private size = \"medium\";\n  private crust = \"regular\";\n  private cheese = true;\n  private toppings: string[] = [];\n\n  withSize(size: string): this {\n    this.size = size;\n    return this;\n  }\n\n  withCrust(crust: string): this {\n    this.crust = crust;\n    return this;\n  }\n\n  withCheese(cheese: boolean): this {\n    this.cheese = cheese;\n    return this;\n  }\n\n  addTopping(topping: string): this {\n    this.toppings.push(topping);\n    return this;\n  }\n\n  build(): Pizza {\n    return new Pizza(this.size, this.crust, this.cheese, [...this.toppings]);\n  }\n}\n\nfunction PizzaPreview({ pizza }: { pizza: Pizza }) {\n  return (\n    <View>\n      <Text>{pizza.size} pizza</Text>\n      <Text>{pizza.crust} crust</Text>\n      <Text>Cheese: {pizza.cheese ? \"yes\" : \"no\"}</Text>\n      <Text>Toppings: {pizza.toppings.join(\", \") || \"none\"}</Text>\n    </View>\n  );\n}\n\nexport function App() {\n  const pizza = useMemo(\n    () =>\n      new PizzaBuilder()\n        .withSize(\"large\")\n        .withCrust(\"thin\")\n        .addTopping(\"pepperoni\")\n        .addTopping(\"mushrooms\")\n        .build(),\n    [],\n  );\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Pizza Builder</Text>\n        <PizzaPreview pizza={pizza} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native version uses the same builder chain, but presents the built pizza in a mobile-friendly layout.",
};
