import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Coffee customization",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\ninterface Coffee {\n  getDescription(): string;\n  getCost(): number;\n}\n\nclass SimpleCoffee implements Coffee {\n  getDescription(): string {\n    return \"Simple coffee\";\n  }\n\n  getCost(): number {\n    return 3;\n  }\n}\n\nabstract class CoffeeDecorator implements Coffee {\n  constructor(protected coffee: Coffee) {}\n\n  getDescription(): string {\n    return this.coffee.getDescription();\n  }\n\n  getCost(): number {\n    return this.coffee.getCost();\n  }\n}\n\nclass MilkDecorator extends CoffeeDecorator {\n  getDescription(): string {\n    return `${super.getDescription()}, milk`;\n  }\n\n  getCost(): number {\n    return super.getCost() + 1;\n  }\n}\n\nclass MochaDecorator extends CoffeeDecorator {\n  getDescription(): string {\n    return `${super.getDescription()}, mocha`;\n  }\n\n  getCost(): number {\n    return super.getCost() + 2;\n  }\n}\n\nclass WhipDecorator extends CoffeeDecorator {\n  getDescription(): string {\n    return `${super.getDescription()}, whip`;\n  }\n\n  getCost(): number {\n    return super.getCost() + 1;\n  }\n}\n\nfunction CoffeeSummary({ coffee }: { coffee: Coffee }) {\n  return (\n    <View style={{ gap: 8 }}>\n      <Text style={{ fontSize: 18 }}>{coffee.getDescription()}</Text>\n      <Text style={{ fontSize: 16 }}>${coffee.getCost().toFixed(2)}</Text>\n    </View>\n  );\n}\n\nexport function App() {\n  const order = useMemo(\n    () => new WhipDecorator(new MochaDecorator(new MilkDecorator(new SimpleCoffee()))),\n    []\n  );\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Coffee Order</Text>\n        <CoffeeSummary coffee={order} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native version uses the same layered decorator structure, but renders the customized coffee with mobile-friendly components.",
};
