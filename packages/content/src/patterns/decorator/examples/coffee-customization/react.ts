import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const react: PatternLanguageExample = {
  language: "react",
  title: "Coffee customization",
  code: "import React, { useMemo } from \"react\";\n\ninterface Coffee {\n  getDescription(): string;\n  getCost(): number;\n}\n\nclass SimpleCoffee implements Coffee {\n  getDescription(): string {\n    return \"Simple coffee\";\n  }\n\n  getCost(): number {\n    return 3;\n  }\n}\n\nabstract class CoffeeDecorator implements Coffee {\n  constructor(protected coffee: Coffee) {}\n\n  getDescription(): string {\n    return this.coffee.getDescription();\n  }\n\n  getCost(): number {\n    return this.coffee.getCost();\n  }\n}\n\nclass MilkDecorator extends CoffeeDecorator {\n  getDescription(): string {\n    return `${super.getDescription()}, milk`;\n  }\n\n  getCost(): number {\n    return super.getCost() + 1;\n  }\n}\n\nclass MochaDecorator extends CoffeeDecorator {\n  getDescription(): string {\n    return `${super.getDescription()}, mocha`;\n  }\n\n  getCost(): number {\n    return super.getCost() + 2;\n  }\n}\n\nclass WhipDecorator extends CoffeeDecorator {\n  getDescription(): string {\n    return `${super.getDescription()}, whip`;\n  }\n\n  getCost(): number {\n    return super.getCost() + 1;\n  }\n}\n\nfunction CoffeeSummary({ coffee }: { coffee: Coffee }) {\n  return (\n    <div>\n      <p>{coffee.getDescription()}</p>\n      <p>${coffee.getCost().toFixed(2)}</p>\n    </div>\n  );\n}\n\nexport function App() {\n  const order = useMemo(\n    () => new WhipDecorator(new MochaDecorator(new MilkDecorator(new SimpleCoffee()))),\n    []\n  );\n\n  return (\n    <main>\n      <h1>Coffee Order</h1>\n      <CoffeeSummary coffee={order} />\n    </main>\n  );\n}",
  explanation: "The React example composes decorators around a base coffee object, so the UI can display a customized drink without needing a separate component for every combination.",
};
