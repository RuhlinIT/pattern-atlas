import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Form state memento",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\n\nclass FormMemento {\n  constructor(\n    public readonly name: string,\n    public readonly email: string,\n    public readonly step: number\n  ) {}\n}\n\n\nclass FormWizard {\n  private name = \"\";\n  private email = \"\";\n  private step = 1;\n\n\n  setName(name: string): void {\n    this.name = name;\n  }\n\n\n  setEmail(email: string): void {\n    this.email = email;\n  }\n\n\n  nextStep(): void {\n    this.step += 1;\n  }\n\n\n  save(): FormMemento {\n    return new FormMemento(this.name, this.email, this.step);\n  }\n\n\n  restore(memento: FormMemento): void {\n    this.name = memento.name;\n    this.email = memento.email;\n    this.step = memento.step;\n  }\n\n\n  summary(): string {\n    return `Name: ${this.name}, Email: ${this.email}, Step: ${this.step}`;\n  }\n}\n\n\nfunction FormPreview({ wizard }: { wizard: FormWizard }) {\n  return (\n    <View>\n      <Text>{wizard.summary()}</Text>\n    </View>\n  );\n}\n\n\nexport function App() {\n  const wizard = useMemo(() => new FormWizard(), []);\n\n\n  useMemo(() => {\n    wizard.setName(\"Ava\");\n    wizard.setEmail(\"ava@example.com\");\n    wizard.nextStep();\n  }, [wizard]);\n\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Form State Memento</Text>\n        <FormPreview wizard={wizard} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native example preserves multi-step form progress with mementos and renders the current draft state in the UI.",
};
