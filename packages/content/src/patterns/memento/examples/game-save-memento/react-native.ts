import type { PatternLanguageExample } from "@atlas-patterns/schemas";

export const reactNative: PatternLanguageExample = {
  language: "react-native",
  title: "Game save memento",
  code: "import React, { useMemo } from \"react\";\nimport { SafeAreaView, Text, View } from \"react-native\";\n\n\nclass GameMemento {\n  constructor(\n    public readonly level: number,\n    public readonly score: number,\n    public readonly lives: number\n  ) {}\n}\n\n\nclass Game {\n  private level = 1;\n  private score = 0;\n  private lives = 3;\n\n\n  play(points: number): void {\n    this.score += points;\n  }\n\n\n  advanceLevel(): void {\n    this.level += 1;\n  }\n\n\n  loseLife(): void {\n    this.lives -= 1;\n  }\n\n\n  save(): GameMemento {\n    return new GameMemento(this.level, this.score, this.lives);\n  }\n\n\n  restore(memento: GameMemento): void {\n    this.level = memento.level;\n    this.score = memento.score;\n    this.lives = memento.lives;\n  }\n\n\n  status(): string {\n    return `Level ${this.level}, Score ${this.score}, Lives ${this.lives}`;\n  }\n}\n\n\nfunction GamePreview({ game }: { game: Game }) {\n  return (\n    <View>\n      <Text>{game.status()}</Text>\n    </View>\n  );\n}\n\n\nexport function App() {\n  const game = useMemo(() => new Game(), []);\n\n\n  useMemo(() => {\n    game.play(100);\n    game.advanceLevel();\n  }, [game]);\n\n\n  return (\n    <SafeAreaView style={{ flex: 1, justifyContent: \"center\", padding: 24 }}>\n      <View style={{ gap: 16 }}>\n        <Text style={{ fontSize: 24, fontWeight: \"600\" }}>Game Save Memento</Text>\n        <GamePreview game={game} />\n      </View>\n    </SafeAreaView>\n  );\n}",
  explanation: "The React Native example shows the current game status while mementos preserve checkpoints behind the scenes.",
};
